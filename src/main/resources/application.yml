spring:
  datasource:
    url: jdbc:mysql://localhost:3306/liveclass?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-test-query: SELECT 1
      keepalive-time: 300000
      max-lifetime: 600000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

enrollment:
  cancellation-deadline-days: 7
